#include<bits/extc++.h>
using namespace std;
namespace pbds=__gnu_pbds;
using ui=unsigned int;
using uli=unsigned long long int;
using li=long long int;
const unordered_map<string,char> WEEK={{"Monday",1},{"Tuesday",2},{"Wednesday",3},{"Thursday",4},{"Friday",5},{"Saturday",6},{"Sunday",7}};
struct said{
    enum {IS,ISNOT,TODAY} type;
    size_t owner,value;
    said(string s){
        if (regex_match(s,regex("")))
    }
};
unordered_map<string,size_t> student;
int main(void){
    ios::sync_with_stdio(false),cin.tie(nullptr),cout.tie(nullptr);
    size_t m,n,p;
    return 0;
}